# TerraGrip-VLA 专属规则。
# 通用的 Python / 编辑器 / 机密规则在仓库根目录的 .gitignore 里，这里不重复。

# ----------------------------------------------------------------------------
# artifacts/ —— 全部产物，一律不进版本库
# ----------------------------------------------------------------------------
# 里面是：合成数据集(.npz)、冻结骨干的特征缓存(.pt)、模型权重(.pt)、
# 保形参数(.json)、评测结果(.json/.csv)、以及所有图(.png)。
#
# 它们【全部都能重建】，而且是逐位可复现的：数据由固定种子生成，特征缓存按
# 图像内容哈希键控。所以提交它们只会让仓库变大，不会带来任何信息。
#
#     python scripts/verify_framework.py --config small   # 重建全部产物 + 逐条验证
#     python -m eval.compare --train-missing              # 重建主表与对比图
artifacts/

# ----------------------------------------------------------------------------
# 如果你确实想把某次跑出来的论文图钉进版本库（比如投稿快照），
# 用 -f 强制加：
#     git add -f artifacts/small/figures/A1_leakage.png
# 或者把这一行取消注释，专门放行图：
# !artifacts/**/figures/*.png
# ----------------------------------------------------------------------------

# 训练时的临时缓存
.pytest_cache/
